global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    # Configure integracoes reais (Slack/Email/PagerDuty) no deploy.
    # Em dev, voce pode observar alertas em:
    # - Prometheus UI: http://localhost:9090/alerts
    # - Alertmanager UI: http://localhost:9093

