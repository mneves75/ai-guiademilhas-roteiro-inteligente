// Vitest stub for Next.js `server-only`.
// In Next.js, importing `server-only` is a compile-time guard against accidental
// client imports. In Vitest/Vite, the real package throws at runtime, which
// breaks unit tests for server modules.
export {};
